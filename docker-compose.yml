version: "3.9"

services:
  scraper:
    build: .
    container_name: bakkal_scraper
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: python main.py
    restart: "no"
